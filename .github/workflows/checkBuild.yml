name: Check build

on:
  pull_request:

jobs:
  check-build:
    name: Build apk for checking code
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v2
        with:
          distribution: 'adopt'
          java-version: '16'
      - env:
          ENCODED_STRING: ${{ secrets.PROPERTIES_B64 }}
        run: echo -e $ENCODED_STRING  | base64 -di > ${{ github.workspace}}/github-keystore.properties

      - env:
          ENCODED_STRING: ${{ secrets.JKS_B64 }}
        run: echo -e $ENCODED_STRING  | base64 -di > ${{ github.workspace}}/github.jks

      - name: Set gradlew executable
        run: chmod +x ./gradlew

      - name: Use Detekt
        run: ./gradlew build
        id: build
        continue-on-error: true

      - if: ${{ steps.build.outcome == 'success' }}
        uses: golfzaptw/action-auto-reviews-from-branches@master
        with:
          GITHUB_TOKEN: ${{ secrets.BOT_TOKEN }}
          EVENT_TYPE: APPROVE
          BRANCHES: feature/*
          AUTHOR: 'Detekt[bot]'
          MESSAGE: Detekt run without problems.

      - if: ${{ steps.build.outcome != 'success' }}
        id: failure
        uses: juliangruber/read-file-action@v1.1.4
        with:
          path: ./app/allapis/build/reports/detekt/detekt.md

      - if: ${{ steps.detekt.outcome != 'success' }}
        uses: golfzaptw/action-auto-reviews-from-branches@master
        with:
          GITHUB_TOKEN: ${{ secrets.BOT_TOKEN }}
          EVENT_TYPE: REQUEST_CHANGES
          BRANCHES: feature/*
          AUTHOR: 'Detekt[bot]'
          MESSAGE: '${{steps.failure.outputs.content}}'

      - name: Compare version of last release and current build
        id: last_release
        uses: InsonusK/get-latest-release@v1.0.1
        with:
          myToken: ${{ secrets.TOKEN }}
          exclude_types: "release"
          view_top: 1

      - id: compare_result
        run: ./gradlew compareVersions -PtagVersion='${{ steps.last_release.outputs.tag_name }}' | tail -1

      - if: ${{ steps.compare_result.outputs.content == 'false' }}
        uses: golfzaptw/action-auto-reviews-from-branches@master
        with:
          GITHUB_TOKEN: ${{ secrets.BOT_TOKEN }}
          EVENT_TYPE: REQUEST_CHANGES
          AUTHOR: 'VersionChecker[bot]'
          MESSAGE: 'Please, change version of new build'

      - if: ${{ steps.compare_result.outputs.content == 'true' }}
        uses: golfzaptw/action-auto-reviews-from-branches@master
        with:
          GITHUB_TOKEN: ${{ secrets.BOT_TOKEN }}
          EVENT_TYPE: APPROVE
          BRANCHES: feature/*
          AUTHOR: 'VersionChecker[bot]'
          MESSAGE: 'Version is good'

      - name: Get versionName
        id: versionName
        run: ./gradlew getVersionName | tail -1

      - name: Check CHANGELOG.md
        id: checkChangelog
        run: grep -q '## ${{ steps.versionName.outputs.content }}' CHANGELOG.md ; echo $?
        continue-on-error: true

      - if: ${{ steps.checkChangelog.outcome != '0' }}
        uses: golfzaptw/action-auto-reviews-from-branches@master
        with:
          GITHUB_TOKEN: ${{ secrets.BOT_TOKEN }}
          EVENT_TYPE: REQUEST_CHANGES
          BRANCHES: feature/*
          AUTHOR: 'ChangelogChecker[bot]'
          MESSAGE: 'Please, add new version to CHANGELOG.md'

      - if: ${{ steps.checkChangelog.outputs.content == '0' }}
        uses: golfzaptw/action-auto-reviews-from-branches@master
        with:
          GITHUB_TOKEN: ${{ secrets.BOT_TOKEN }}
          EVENT_TYPE: APPROVE
          BRANCHES: feature/*
          AUTHOR: 'ChangelogChecker[bot]'
          MESSAGE: 'CHANGELOG.md is good'
